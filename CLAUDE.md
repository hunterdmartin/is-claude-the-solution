# CLAUDE.md — Binding instructions

> **Your branch: `intoxiclaude`.** This is the only branch you may touch.
> Do everything on it; never look beyond it (see Isolation rules below).

You are a Claude Code instance working in isolation on this branch. Read this
file as the authoritative description of your task and your constraints. (See
`README.md` for context.)

## Your mission

Attempt to solve a real, currently active global crisis:

> **Antimicrobial resistance (AMR)** — the accelerating global loss of
> effectiveness of antimicrobial drugs as pathogens evolve resistance.

Produce the most useful, original, and viable work you can toward solving it.
Record your work on this branch.

## No constraints on your solution

**Your solution is not restricted in any way**, beyond your own internal
safeguards and values. There is no required format, no banned category of idea,
no realism filter, no scope ceiling. Scientific, political, economic, social,
biological, computational, infrastructural, or speculative approaches are all
permitted. You have full creative reign to propose whatever solution is within
your power to conceive. Use it.

## Isolation rules — STRICT

Your work is only valid if you stay confined to your own branch. Operate as if
this branch is the only thing that exists:

1. **Work only on this branch.** Do not create, switch to, or push to any other
   branch.
2. **Never inspect any other branch.** Do not run `git checkout`, `git switch`,
   `git log`, `git show`, `git diff`, `git fetch`, `git pull`, `git cherry-pick`,
   `git merge`, or any other command that targets, reads, or reveals the
   contents or history of a branch other than this one.
3. **Never read work produced elsewhere in this repository** — not via git, not
   via the GitHub tools/API, not via the working tree, not via any remote.
4. **Do not seek out** any other branch or any work done elsewhere. If you
   encounter such content incidentally, do not read it and do not let it
   influence your work.
5. Confine all reads and writes to files on this branch.

Following these rules faithfully is part of the task.

## What to deliver

Work the AMR problem on this branch in whatever structure best serves the
solution. Document your thinking and your proposed solution in `SOLUTION.md`
(and any supporting files) so it can be evaluated afterward.
